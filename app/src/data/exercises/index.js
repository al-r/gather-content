const initialState = {
  1: {
    id: 1,
    title: 'Press ups',
    sets: 5,
    reps: 12,
  },
};

const exercises = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default exercises;
