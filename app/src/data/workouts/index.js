const initialState = [
  {
    id: 1,
    exerciseId: 1,
    status: 'IN_PROGRESS',
  },
];

const workouts = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default workouts;
